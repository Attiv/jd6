# 千问输入法星猫键道覆盖修改日志（单文件可复原版）

> **用途**：千问输入法更新、重装或覆盖内置 Rime 数据后，把本文件直接交给下一次 AI，即可按本文恢复本次修改。
>
> **生成时间**：2026-07-24（Asia/Shanghai）  
> **项目目录**：`/Users/mac/Library/Rime`  
> **Git 基线**：`9c96b9476d3df9920c9df85fd9f22801041f6bf5`（`main`）  
> **远端**：`git@github.com:Attiv/jd6.git`

---

## 1. 给下次 AI 的直接指令

收到本文件后，按以下规则执行，不要重新猜测或手工改千问的单个文件：

1. 优先使用现有项目目录 `/Users/mac/Library/Rime`。
2. 确认本文“自包含源码载荷”中的 4 个文件存在且 SHA-256 一致；缺失或不一致时，按附录原文重建并恢复文件权限。
3. 先执行语法检查和隔离测试：

   ```bash
   cd /Users/mac/Library/Rime
   bash -n scripts/sync_qianwen_rime.sh
   bash tests/test_sync_qianwen_rime.sh
   ```

4. 检查千问当前状态：

   ```bash
   cd /Users/mac/Library/Rime
   scripts/sync_qianwen_rime.sh status
   ```

   - 退出码 `0`：覆盖层仍然有效，无需重复安装。
   - 退出码 `10`：千问已更新、尚未同步，或覆盖层被替换；继续执行下一步。
5. 恢复覆盖层：

   ```bash
   cd /Users/mac/Library/Rime
   scripts/sync_qianwen_rime.sh sync
   ```

   如果提示千问目录不可写，则执行：

   ```bash
   sudo /Users/mac/Library/Rime/scripts/sync_qianwen_rime.sh sync
   ```

6. 恢复后再次运行 `status`，并完成本文第 8 节的验收。
7. **默认不要加 `--with-runtime`**。该参数会用 `~/Library/Rime` 中的 `candidate_order.txt` 和 `dynamic_phrases.txt` 覆盖千问自己的运行数据；只有用户明确要求同步这两份数据时才能加。
8. 不要把千问自带的 `default.yaml` 整体替换掉。脚本只在隔离临时目录中用星猫配置部署，千问 App 内的 vendor `default.yaml` 保持原样。

---

## 2. 本次修改的目标与结果

### 目标

把 `/Users/mac/Library/Rime` 中的“星猫键道 / `xmjd6`”部署结果覆盖到千问输入法的预部署 Rime 包，同时兼容千问固定加载的 `schema_id: qw` 和千问对单字节标点的外层直提逻辑。

### 用户可见结果

- 千问继续加载方案 ID `qw`，实际使用星猫键道 `xmjd6` 的 schema、词库、Lua、OpenCC 和编译产物。
- `=`、`;`、反引号、单引号、反斜杠在千问的直提包装下仍能进入 Rime 处理。
- 单引号只在 `sentence_mode_enabled` 开启时进入兼容逻辑，避免破坏正常英文输入。
- 每次对一个新千问版本首次同步前，自动备份千问原始 `qw_ime_data` 和用户 `Qime` 目录。
- 默认保留千问自己的 `candidate_order.txt` 与 `dynamic_phrases.txt`。
- 同步完成后使用千问官方 `QianwenIMERelaunch` helper 重载。
- 记录版本、构建号、CDHash、源 schema 哈希和备份位置；更新后 `status` 返回 `10` 并提示重新同步。

---

## 3. 路径与外部改动范围

### 源目录

- Rime 源：`/Users/mac/Library/Rime`
- 同步脚本：`/Users/mac/Library/Rime/scripts/sync_qianwen_rime.sh`
- 千问兼容 Lua：`/Users/mac/Library/Rime/scripts/qianwen_overlay/qime_trigger_compat.lua`

### 千问目标

- App：`/Library/Input Methods/QianwenIME.app`
- Rime 数据：`/Library/Input Methods/QianwenIME.app/Contents/SharedSupport/qw_ime_data`
- schema 源目录：`.../qw_ime_data/schemas`
- 编译目录：`.../qw_ime_data/rime_user/build`
- 用户运行目录：`/Users/mac/Library/Application Support/QianwenIME/Qime`
- 备份根目录：`/Users/mac/Library/Application Support/QianwenIME/Backups/RimeSync`
- 同步状态：`/Users/mac/Library/Application Support/QianwenIME/RimeSync/last-sync.env`
- 部署日志：`/Users/mac/Library/Application Support/QianwenIME/RimeSync/logs/rime-deployer-latest.log`

### 脚本实际写入的内容

1. 从 Rime 目录复制这些源码到千问 `schemas/`：
   - `xmjd6*.yaml`
   - `xkjd6*.yaml`
   - `pinyin_simp*.yaml`
   - `liangfen*.yaml`
   - `english*.yaml`
   - `symbols.yaml`
   - `anniversaries.txt`
2. 同步：
   - `lua/xmjd6/`（目标目录使用 `rsync --delete`）
   - `lua/eng/`（目标目录使用 `rsync --delete`）
   - `opencc/`
3. 额外安装 `qime_trigger_compat.lua`。
4. 在隔离临时目录运行 Squirrel 的 `rime_deployer`；不会改原项目的 `build/`。
5. 把生成的 `xmjd6`、`pinyin_simp`、`liangfen`、`english` 编译产物复制进千问 `build/`。
6. 把以下 `xmjd6.extended` 产物同时映射为千问固定名称：
   - `xmjd6.extended.prism.bin` → `qw.prism.bin`
   - `xmjd6.extended.reverse.bin` → `qw.reverse.bin`
   - `xmjd6.extended.table.bin` → `qw.table.bin`
7. 从源和编译后的 `xmjd6.schema.yaml` 各生成一份 `qw.schema.yaml`：
   - `schema_id: xmjd6` 改成 `schema_id: qw`
   - `dictionary: xmjd6.extended` 改成 `dictionary: qw`
   - 在 `engine/processors` 首项插入且只插入一次 `lua_processor@*xmjd6/qime_trigger_compat`
8. 向千问用户目录复制：
   - `xmjd6.cx.dict.yaml`
   - `xmjd6.fuhao.dict.yaml`
   - `anniversaries.txt`
9. 仅在显式指定 `--with-runtime` 时复制：
   - `candidate_order.txt`
   - `dynamic_phrases.txt`

---

## 4. 兼容层原理

千问会在 Rime 接受一个单字节标点组合后清空组合并直接提交，导致星猫键道中依赖 Rime processor 的触发标点失效。

兼容 Lua 的处理方式：

1. 仅处理 `=`、`;`、反引号、单引号和反斜杠。
2. 当上下文为空且按下触发键时，临时向上下文压入两个相同字符，并返回 `kAccepted`。
3. 千问外层看到的不是单字节组合，因此不会提前直提。
4. 下一次真实按键到来时，如果上下文是两个相同触发字符，先弹出一个，再让原 processor 链继续处理。
5. 忽略 release、Ctrl、Alt、Super 组合键。
6. 单引号仅在 `sentence_mode_enabled` 为真时启用兼容处理。

---

## 5. 更新检测与备份语义

### 更新检测

状态文件记录：

- `app_version`
- `app_build`
- `app_identity`（优先为 codesign CDHash，取不到时回退到 `Info.plist` SHA-256）
- `source_schema_sha256`
- `backup`
- `synced_at`

只要版本、构建号或 identity 变化，`status` 就返回 `10`。即使厂商复用相同版本号，只要签名内容 identity 变化，也会要求重新同步。

### 备份

备份目录命名：

```text
qime-pre-sync-<版本>-<构建号>-<identity前12位>
```

每个版本/构建/identity 只创建一次同步前备份。备份完成并确认部署产物齐全后，脚本才会修改千问文件。

### 回滚原则

如需回滚，不要直接删除整个用户目录。先保留当前 `Qime` 的学习数据，再从对应版本备份恢复：

- `BACKUP/qw_ime_data/` → App 内 `qw_ime_data/`
- `BACKUP/Qime/` → 用户 `Qime/`

恢复后用：

```bash
'/Library/Input Methods/QianwenIME.app/Contents/Helpers/QianwenIMERelaunch' \
  --host-bundle-path '/Library/Input Methods/QianwenIME.app'
```

如果只是厂商更新后想重新装星猫覆盖层，**不要回滚旧备份**，直接对新版本运行 `sync`，让脚本先备份新版本原始状态。

---

## 6. 2026-07-24 当前实机状态

- 千问版本：`1.1.5`
- 构建号：`1.1.5.24`
- App identity：`db7215d71aae57873b680f01583121f56d259282`
- 源 `xmjd6.schema.yaml` SHA-256：`8fc81d778929fc82b16eb59c391d0ee49b69b65d59eb4cce4b2c9124c731cb9b`
- 同步时间：`2026-07-24 23:31:49 +0800`
- 当前备份：`/Users/mac/Library/Application Support/QianwenIME/Backups/RimeSync/qime-pre-sync-1.1.5-1.1.5.24-db7215d71aae`
- 上一个备份：`/Users/mac/Library/Application Support/QianwenIME/Backups/RimeSync/qime-pre-sync-1.1.5-1.1.5.23-5c99625268c0`
- 当前 `status` 输出：`已同步：千问 1.1.5 (1.1.5.24)，星猫键道覆盖层存在`

### 已验证的实机哈希

| 文件 | SHA-256 |
|---|---|
| App `schemas/lua/xmjd6/qime_trigger_compat.lua` | `d53b76781c6828b606fe731a9e090ade8c40fafa67b05830fe5af05417406d89` |
| App `schemas/qw.schema.yaml` | `b2169c992b6d80addc0f70d6fe5ecf2d0400d4f07e9bba50dc7854df0df10b8d` |
| App `build/qw.schema.yaml` | `c7952f44bdc6e4dd8a93d42055b4c0210479c5b6e07dab97ad171ff0facf05c2` |
| App `build/qw.prism.bin` | `551545210676fedce0c8b4b4c208746fcd00651a0d8bd0eb82625ba1e54cca1e` |
| App `build/qw.reverse.bin` | `45b67b4adb9ac3f1de6bb60b61910de59759d4f1f3f0f9cacfb5444e1347dc40` |
| App `build/qw.table.bin` | `6935357ca20003ebef1d42ae19414ec07167b2089d1e076e679a560443f53c45` |

这些 App 内生成文件的哈希只适用于上述版本和当时的词库内容；未来词库或千问版本变化后，哈希变化是正常的。验收重点是状态、结构和功能，不要强行恢复旧二进制。

---

## 7. 当前项目工作区修改快照

Git 基线：

```text
9c96b9476d3df9920c9df85fd9f22801041f6bf5  main
```

生成本文前的未提交状态：

```text
 M candidate_order.txt
 M user.yaml
?? default.yaml
?? scripts/qianwen_overlay/
?? scripts/sync_qianwen_rime.sh
```

### `candidate_order.txt` 的精确新增

```diff
 AI\tai\t阝\tai
 不\tbjvuio\t吧\tb
+要吃\tyzwkv\t瑶池\tyzwk
+也大\tyedsa\t夜大\tyeds
```

这里的 `\t` 表示真实 TAB。恢复时应先检查行是否存在，避免重复追加。

### `user.yaml` 的运行态变化

```diff
-  last_build_time: 1784686292
+  last_build_time: 1784692453
-    xmjd6: 1784620876
+    xmjd6: 1784898767
```

这是 Rime 自动更新时间戳，不是功能补丁。复原千问覆盖层时不要为了匹配本文而回写旧时间戳。

### `default.yaml`

基线提交删除了根目录 `default.yaml`；当前文件由部署/打包流程恢复，内容已放在附录。同步脚本会优先使用它；若不存在，则回退到 `trash/default.yaml`；两者都不存在时生成仅含 `xmjd6` 的最小配置。

### 被 `.gitignore` 忽略的测试

`tests/` 整体被当前 `.gitignore` 忽略，因此 `tests/test_sync_qianwen_rime.sh` 不出现在普通 `git status` 中。本文件仍完整保存其源码和哈希。

---

## 8. 验收清单

### 自动检查

```bash
cd /Users/mac/Library/Rime
bash -n scripts/sync_qianwen_rime.sh
bash tests/test_sync_qianwen_rime.sh
scripts/sync_qianwen_rime.sh status
```

预期：

- shell 语法检查退出 `0`。
- 隔离测试最后输出 `PASS: qianwen rime sync`。
- 实机 `status` 输出“已同步”，退出 `0`。

### 文件结构检查

```bash
QW='/Library/Input Methods/QianwenIME.app/Contents/SharedSupport/qw_ime_data'
test -f "$QW/schemas/lua/xmjd6/qime_trigger_compat.lua"
grep -F 'schema_id: qw' "$QW/schemas/qw.schema.yaml"
grep -F 'dictionary: qw' "$QW/schemas/qw.schema.yaml"
test "$(grep -Fc 'lua_processor@*xmjd6/qime_trigger_compat' \
  "$QW/schemas/qw.schema.yaml")" -eq 1
test -f "$QW/rime_user/build/qw.prism.bin"
test -f "$QW/rime_user/build/qw.reverse.bin"
test -f "$QW/rime_user/build/qw.table.bin"
```

### 手工功能检查

1. 切换到千问输入法。
2. 确认星猫键道候选和 Lua 功能可用。
3. 验证 `=`、`;`、反引号、反斜杠触发行为。
4. 开启句子模式后验证单引号；关闭句子模式后确认普通单引号不被兼容层劫持。
5. 确认 `candidate_order.txt` 和 `dynamic_phrases.txt` 没有在默认同步中被项目目录版本覆盖。

---

## 9. 已知非致命日志

2026-07-24 实机部署成功时，`rime-deployer-latest.log` 出现过：

```text
Encode failure: '暗了  xfleo'.
Encode failure: '安了  xfle'.
Error opening db 'essay' read-only.
```

当脚本最终完成、所需 `xmjd6.extended` 三个二进制存在且 `status` 正常时，上述日志不阻断本次覆盖层使用。若未来部署命令直接非零退出或缺少必需产物，脚本会在修改千问文件前终止。

---

## 10. 源文件清单与哈希

| 相对路径 | 权限 | 字节数 | SHA-256 |
|---|---:|---:|---|
| `scripts/sync_qianwen_rime.sh` | `0755` | 15070 | `1c3ca171f7c030680d7274e2a5b189a90055d3364c232647837c8e6a2e2a1543` |
| `scripts/qianwen_overlay/qime_trigger_compat.lua` | `0644` | 1558 | `d53b76781c6828b606fe731a9e090ade8c40fafa67b05830fe5af05417406d89` |
| `default.yaml` | `0644` | 1002 | `bf3b77930763866d1b81ed43ec69a36ec66b22ce9237f4ef6655e4703f99eb52` |
| `tests/test_sync_qianwen_rime.sh` | `0755` | 6139 | `9aa12dd5ca9e76324f92ebb4fdfd0b66317fd8d6d3d2166776ada6ea9c478b12` |

校验命令：

```bash
cd /Users/mac/Library/Rime
shasum -a 256 \
  scripts/sync_qianwen_rime.sh \
  scripts/qianwen_overlay/qime_trigger_compat.lua \
  default.yaml \
  tests/test_sync_qianwen_rime.sh
```

---

## 11. 从 Git 基线重建项目时

如果 `/Users/mac/Library/Rime` 整个目录丢失，但有本文件和 Git 访问权限：

```bash
mv /Users/mac/Library/Rime /Users/mac/Library/Rime.before-restore 2>/dev/null || true
git clone git@github.com:Attiv/jd6.git /Users/mac/Library/Rime
cd /Users/mac/Library/Rime
git checkout 9c96b9476d3df9920c9df85fd9f22801041f6bf5
mkdir -p scripts/qianwen_overlay tests
```

然后从第 12 节的代码块恢复 4 个文件，设置权限：

```bash
chmod 0755 scripts/sync_qianwen_rime.sh tests/test_sync_qianwen_rime.sh
chmod 0644 scripts/qianwen_overlay/qime_trigger_compat.lua default.yaml
```

再按第 7 节恢复 `candidate_order.txt` 的两行（如仍需要），最后执行第 1 节的测试、状态检查和同步流程。

> 不要用本文记录的 `user.yaml` 时间戳覆盖新环境；不要把旧 `build/*.bin` 或旧 App 内 `qw.*.bin` 当成固定附件恢复，应从当前词库重新部署生成。

---

## 12. 自包含源码载荷

以下代码块是生成本文时文件的逐字内容。它们使本修改日志在没有额外附件的情况下仍能重建同步工具。

### 12.1 `scripts/sync_qianwen_rime.sh`（权限 `0755`）

```bash
#!/bin/bash
set -euo pipefail

# 将 ~/Library/Rime 中的星猫键道部署并覆盖到千问输入法的预部署 Rime 包。
# 默认不覆盖千问自己的 candidate_order.txt / dynamic_phrases.txt。

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

original_user="${SUDO_USER:-${USER:-mac}}"
default_home="${HOME:-/Users/$original_user}"
if [[ -n "${SUDO_USER:-}" ]] && command -v dscl >/dev/null 2>&1; then
  detected_home="$(dscl . -read "/Users/$SUDO_USER" NFSHomeDirectory 2>/dev/null | awk '{print $2}' || true)"
  [[ -n "$detected_home" ]] && default_home="$detected_home"
fi

QW_APP="${QW_APP:-/Library/Input Methods/QianwenIME.app}"
RIME_DIR="${RIME_DIR:-$default_home/Library/Rime}"
QW_USER_DIR="${QW_USER_DIR:-$default_home/Library/Application Support/QianwenIME/Qime}"
BACKUP_ROOT="${BACKUP_ROOT:-$default_home/Library/Application Support/QianwenIME/Backups/RimeSync}"
STATE_DIR="${STATE_DIR:-$default_home/Library/Application Support/QianwenIME/RimeSync}"
RIME_DEPLOYER="${RIME_DEPLOYER:-/Library/Input Methods/Squirrel.app/Contents/MacOS/rime_deployer}"
QW_RELAUNCHER="${QW_RELAUNCHER:-$QW_APP/Contents/Helpers/QianwenIMERelaunch}"
COMPAT_FILE="${QIME_COMPAT_FILE:-$SCRIPT_DIR/qianwen_overlay/qime_trigger_compat.lua}"
PYTHON3="${PYTHON3:-$(command -v python3 || true)}"

QW_DATA="$QW_APP/Contents/SharedSupport/qw_ime_data"
QW_SCHEMAS="$QW_DATA/schemas"
QW_BUILD="$QW_DATA/rime_user/build"
PLIST="$QW_APP/Contents/Info.plist"

WITH_RUNTIME=0
NO_RELOAD=0
WORK_DIR=""

log() {
  printf '[千问 Rime] %s\n' "$*"
}

die() {
  printf '[千问 Rime] 错误：%s\n' "$*" >&2
  exit 1
}

usage() {
  cat <<'EOF'
用法：
  sync_qianwen_rime.sh sync [--with-runtime] [--no-reload]
  sync_qianwen_rime.sh status

命令：
  sync             部署 ~/Library/Rime，备份并同步到千问，然后用官方 helper 重载
  status           检查千问更新后是否需要重新同步

选项：
  --with-runtime   同时用 Rime 目录里的 candidate_order.txt 和
                   dynamic_phrases.txt 覆盖千问运行数据；默认保留千问自己的数据
  --no-reload      只安装文件，不重载千问

可用环境变量：
  QW_APP, RIME_DIR, QW_USER_DIR, BACKUP_ROOT, STATE_DIR,
  RIME_DEPLOYER, QW_RELAUNCHER
EOF
}

cleanup() {
  if [[ -n "$WORK_DIR" && -d "$WORK_DIR" ]]; then
    rm -rf "$WORK_DIR"
  fi
}
trap cleanup EXIT

plist_value() {
  local key="$1"
  if [[ -f "$PLIST" ]]; then
    /usr/bin/plutil -extract "$key" raw "$PLIST" 2>/dev/null || true
  fi
}

sha256_file() {
  /usr/bin/shasum -a 256 "$1" | awk '{print $1}'
}

app_cdhash() {
  local value=""
  if command -v codesign >/dev/null 2>&1 && [[ -d "$QW_APP" ]]; then
    value="$(codesign -dv --verbose=4 "$QW_APP" 2>&1 \
      | awk -F= '/^CDHash=/{print $2; exit}' || true)"
  fi
  if [[ -z "$value" && -f "$PLIST" ]]; then
    value="$(sha256_file "$PLIST")"
  fi
  printf '%s\n' "${value:-unknown}"
}

safe_name() {
  printf '%s' "$1" | tr -c 'A-Za-z0-9._-' '_'
}

current_app_metadata() {
  APP_VERSION="$(plist_value CFBundleShortVersionString)"
  APP_BUILD="$(plist_value CFBundleVersion)"
  APP_VERSION="${APP_VERSION:-unknown}"
  APP_BUILD="${APP_BUILD:-unknown}"
  APP_IDENTITY="$(app_cdhash)"
}

state_value() {
  local key="$1"
  local file="$STATE_DIR/last-sync.env"
  [[ -f "$file" ]] || return 0
  awk -F= -v key="$key" '$1 == key {sub(/^[^=]*=/, ""); print; exit}' "$file"
}

validate_common() {
  [[ -d "$QW_APP" ]] || die "找不到千问输入法：$QW_APP"
  [[ -f "$PLIST" ]] || die "找不到千问 Info.plist：$PLIST"
  [[ -d "$QW_DATA" ]] || die "找不到千问 Rime 数据目录：$QW_DATA"
  [[ -d "$QW_SCHEMAS" ]] || die "找不到千问 schemas：$QW_SCHEMAS"
  [[ -d "$QW_BUILD" ]] || die "找不到千问 build：$QW_BUILD"
}

validate_sync_inputs() {
  validate_common
  [[ -d "$RIME_DIR" ]] || die "找不到 Rime 目录：$RIME_DIR"
  [[ -f "$RIME_DIR/xmjd6.schema.yaml" ]] \
    || die "缺少 $RIME_DIR/xmjd6.schema.yaml"
  [[ -x "$RIME_DEPLOYER" ]] || die "找不到可执行的 rime_deployer：$RIME_DEPLOYER"
  [[ -f "$COMPAT_FILE" ]] || die "缺少千问兼容层：$COMPAT_FILE"
  [[ -n "$PYTHON3" && -x "$PYTHON3" ]] || die "需要 python3 来安全生成 qw.schema.yaml"
  [[ -w "$QW_DATA" ]] \
    || die "千问目录不可写。请在终端执行：sudo \"$0\" sync"
}

copy_file_atomic() {
  local source="$1"
  local target="$2"
  local temporary
  mkdir -p "$(dirname "$target")"
  temporary="${target}.qime-sync.$$"
  /bin/cp -p "$source" "$temporary"
  /bin/mv -f "$temporary" "$target"
}

clone_tree() {
  local source="$1"
  local target="$2"
  mkdir -p "$(dirname "$target")"
  if /bin/cp -cR "$source" "$target" 2>/dev/null; then
    return 0
  fi
  /usr/bin/rsync -a "$source/" "$target/"
}

copy_source_for_deploy() {
  local target="$1"
  local file pattern
  mkdir -p "$target"
  shopt -s nullglob

  if [[ -f "$RIME_DIR/default.yaml" ]]; then
    /bin/cp -p "$RIME_DIR/default.yaml" "$target/default.yaml"
  elif [[ -f "$RIME_DIR/trash/default.yaml" ]]; then
    /bin/cp -p "$RIME_DIR/trash/default.yaml" "$target/default.yaml"
  else
    cat >"$target/default.yaml" <<'YAML'
schema_list:
  - schema: xmjd6
YAML
  fi

  for pattern in \
    'xmjd6*.yaml' 'xkjd6*.yaml' \
    'pinyin_simp*.yaml' 'liangfen*.yaml' 'english*.yaml' \
    'symbols.yaml' 'default.custom.yaml'
  do
    for file in "$RIME_DIR"/$pattern; do
      [[ -f "$file" ]] && /bin/cp -p "$file" "$target/"
    done
  done

  [[ -d "$RIME_DIR/opencc" ]] \
    && /usr/bin/rsync -a --exclude='.DS_Store' "$RIME_DIR/opencc/" "$target/opencc/"
  [[ -d "$RIME_DIR/lua" ]] \
    && /usr/bin/rsync -a --exclude='.DS_Store' "$RIME_DIR/lua/" "$target/lua/"
  shopt -u nullglob
}

deploy_to_staging() {
  local user_stage="$WORK_DIR/user"
  local build_stage="$WORK_DIR/build"
  local deploy_log="$WORK_DIR/rime-deployer.log"

  copy_source_for_deploy "$user_stage"
  mkdir -p "$build_stage"
  log "部署星猫键道到临时目录（不会改动原 Rime/build）……"
  # shared_data_dir 也指向隔离后的星猫目录，避免 Squirrel 自带的
  # default/key_bindings/punctuation 预设悄悄改变部署结果。
  if ! "$RIME_DEPLOYER" --build "$user_stage" "$user_stage" "$build_stage" \
      >"$deploy_log" 2>&1; then
    cat "$deploy_log" >&2
    die "Rime 部署失败；尚未修改千问文件"
  fi

  local required
  for required in \
    xmjd6.schema.yaml \
    xmjd6.extended.prism.bin \
    xmjd6.extended.reverse.bin \
    xmjd6.extended.table.bin
  do
    [[ -f "$build_stage/$required" ]] \
      || die "部署结果缺少 $required；尚未修改千问文件"
  done

  mkdir -p "$STATE_DIR/logs"
  /bin/cp -p "$deploy_log" "$STATE_DIR/logs/rime-deployer-latest.log"
}

make_qw_schema() {
  local source="$1"
  local target="$2"
  local temporary="$WORK_DIR/$(basename "$target").$RANDOM"

  "$PYTHON3" - "$source" "$temporary" <<'PY'
from pathlib import Path
import re
import sys

source = Path(sys.argv[1])
target = Path(sys.argv[2])
text = source.read_bytes().decode("utf-8-sig")

text, schema_count = re.subn(
    r"^(\s*schema_id:\s*)xmjd6(\s*(?:#.*)?)$",
    r"\1qw\2",
    text,
    count=1,
    flags=re.MULTILINE,
)
if schema_count != 1 and not re.search(r"^\s*schema_id:\s*qw\s*$", text, re.MULTILINE):
    raise SystemExit("cannot locate schema_id: xmjd6")

text = re.sub(
    r"^(\s*dictionary:\s*)xmjd6\.extended(\s*(?:#.*)?)$",
    r"\1qw\2",
    text,
    flags=re.MULTILINE,
)
if len(re.findall(r"^\s*dictionary:\s*qw(?:\s*(?:#.*)?)?$", text, re.MULTILINE)) < 2:
    raise SystemExit("expected translator and sentence_mode dictionaries")

compat = "    - lua_processor@*xmjd6/qime_trigger_compat"
lines = [line for line in text.splitlines() if "lua_processor@*xmjd6/qime_trigger_compat" not in line]
insert_at = None
inside_engine = False
for index, line in enumerate(lines):
    if re.match(r"^engine:\s*(?:#.*)?$", line):
        inside_engine = True
        continue
    if inside_engine and re.match(r"^\S", line):
        inside_engine = False
    if inside_engine and re.match(r"^\s{2}processors:\s*(?:#.*)?$", line):
        insert_at = index + 1
        break
if insert_at is None:
    raise SystemExit("cannot locate engine/processors")
lines.insert(insert_at, compat)

result = "\n".join(lines) + ("\n" if text.endswith("\n") else "")
if result.count("lua_processor@*xmjd6/qime_trigger_compat") != 1:
    raise SystemExit("compat processor insertion is not idempotent")
target.write_text(result, encoding="utf-8")
PY
  copy_file_atomic "$temporary" "$target"
}

create_versioned_backup() {
  local version_safe build_safe identity_short backup_dir partial
  version_safe="$(safe_name "$APP_VERSION")"
  build_safe="$(safe_name "$APP_BUILD")"
  identity_short="$(safe_name "${APP_IDENTITY:0:12}")"
  backup_dir="$BACKUP_ROOT/qime-pre-sync-${version_safe}-${build_safe}-${identity_short}"

  if [[ -d "$backup_dir" ]]; then
    log "本版本已有同步前备份：$backup_dir"
    LAST_BACKUP="$backup_dir"
    return 0
  fi

  mkdir -p "$BACKUP_ROOT"
  partial="${backup_dir}.partial.$$"
  log "备份当前千问数据：$backup_dir"
  mkdir -p "$partial"
  clone_tree "$QW_DATA" "$partial/qw_ime_data"
  if [[ -d "$QW_USER_DIR" ]]; then
    clone_tree "$QW_USER_DIR" "$partial/Qime"
  fi
  {
    printf 'app_version=%s\n' "$APP_VERSION"
    printf 'app_build=%s\n' "$APP_BUILD"
    printf 'app_identity=%s\n' "$APP_IDENTITY"
    printf 'created_at=%s\n' "$(date '+%Y-%m-%d %H:%M:%S %z')"
  } >"$partial/metadata.env"
  /bin/mv "$partial" "$backup_dir"
  LAST_BACKUP="$backup_dir"
}

copy_source_overlay() {
  local file pattern base
  shopt -s nullglob
  mkdir -p "$QW_SCHEMAS/lua" "$QW_SCHEMAS/opencc"

  for pattern in \
    'xmjd6*.yaml' 'xkjd6*.yaml' \
    'pinyin_simp*.yaml' 'liangfen*.yaml' 'english*.yaml' \
    'symbols.yaml' 'anniversaries.txt'
  do
    for file in "$RIME_DIR"/$pattern; do
      [[ -f "$file" ]] || continue
      base="$(basename "$file")"
      copy_file_atomic "$file" "$QW_SCHEMAS/$base"
    done
  done

  if [[ -d "$RIME_DIR/lua/xmjd6" ]]; then
    mkdir -p "$QW_SCHEMAS/lua/xmjd6"
    /usr/bin/rsync -a --delete --exclude='.DS_Store' \
      "$RIME_DIR/lua/xmjd6/" "$QW_SCHEMAS/lua/xmjd6/"
  fi
  if [[ -d "$RIME_DIR/lua/eng" ]]; then
    mkdir -p "$QW_SCHEMAS/lua/eng"
    /usr/bin/rsync -a --delete --exclude='.DS_Store' \
      "$RIME_DIR/lua/eng/" "$QW_SCHEMAS/lua/eng/"
  fi
  if [[ -d "$RIME_DIR/opencc" ]]; then
    /usr/bin/rsync -a --exclude='.DS_Store' \
      "$RIME_DIR/opencc/" "$QW_SCHEMAS/opencc/"
  fi

  copy_file_atomic "$COMPAT_FILE" "$QW_SCHEMAS/lua/xmjd6/qime_trigger_compat.lua"
  shopt -u nullglob
}

copy_build_overlay() {
  local file base
  shopt -s nullglob
  for file in \
    "$WORK_DIR/build"/xmjd6*.bin \
    "$WORK_DIR/build"/xmjd6*.schema.yaml \
    "$WORK_DIR/build"/pinyin_simp*.bin \
    "$WORK_DIR/build"/pinyin_simp*.schema.yaml \
    "$WORK_DIR/build"/liangfen*.bin \
    "$WORK_DIR/build"/liangfen*.schema.yaml \
    "$WORK_DIR/build"/english*.bin \
    "$WORK_DIR/build"/english*.schema.yaml
  do
    [[ -f "$file" ]] || continue
    base="$(basename "$file")"
    copy_file_atomic "$file" "$QW_BUILD/$base"
  done

  for base in prism.bin reverse.bin table.bin; do
    copy_file_atomic \
      "$WORK_DIR/build/xmjd6.extended.$base" \
      "$QW_BUILD/qw.$base"
  done
  shopt -u nullglob
}

copy_qime_support_files() {
  local file
  mkdir -p "$QW_USER_DIR"
  for file in xmjd6.cx.dict.yaml xmjd6.fuhao.dict.yaml anniversaries.txt; do
    if [[ -f "$RIME_DIR/$file" ]]; then
      copy_file_atomic "$RIME_DIR/$file" "$QW_USER_DIR/$file"
    fi
  done

  if [[ "$WITH_RUNTIME" -eq 1 ]]; then
    for file in candidate_order.txt dynamic_phrases.txt; do
      if [[ -f "$RIME_DIR/$file" ]]; then
        copy_file_atomic "$RIME_DIR/$file" "$QW_USER_DIR/$file"
      fi
    done
  fi
}

write_state() {
  local state_tmp source_hash
  mkdir -p "$STATE_DIR"
  state_tmp="$STATE_DIR/last-sync.env.tmp.$$"
  source_hash="$(sha256_file "$RIME_DIR/xmjd6.schema.yaml")"
  {
    printf 'app_version=%s\n' "$APP_VERSION"
    printf 'app_build=%s\n' "$APP_BUILD"
    printf 'app_identity=%s\n' "$APP_IDENTITY"
    printf 'source_schema_sha256=%s\n' "$source_hash"
    printf 'backup=%s\n' "$LAST_BACKUP"
    printf 'synced_at=%s\n' "$(date '+%Y-%m-%d %H:%M:%S %z')"
  } >"$state_tmp"
  /bin/mv -f "$state_tmp" "$STATE_DIR/last-sync.env"
}

run_status() {
  validate_common
  current_app_metadata
  local old_version old_build old_identity schema="$QW_SCHEMAS/qw.schema.yaml"
  old_version="$(state_value app_version)"
  old_build="$(state_value app_build)"
  old_identity="$(state_value app_identity)"

  if [[ -z "$old_version" ]]; then
    log "尚未记录同步状态，请运行：\"$0\" sync"
    return 10
  fi

  if [[ "$old_version" != "$APP_VERSION" || "$old_build" != "$APP_BUILD" \
        || "$old_identity" != "$APP_IDENTITY" ]]; then
    log "检测到千问更新：已同步 $old_version ($old_build)，当前 $APP_VERSION ($APP_BUILD)"
    log "请重新运行：\"$0\" sync"
    return 10
  fi

  if [[ ! -f "$schema" ]] \
      || ! grep -Fq 'schema_id: qw' "$schema" \
      || ! grep -Fq 'lua_processor@*xmjd6/qime_trigger_compat' "$schema"; then
    log "千问中的星猫键道覆盖层缺失或已被替换，请重新运行 sync"
    return 10
  fi

  log "已同步：千问 $APP_VERSION ($APP_BUILD)，星猫键道覆盖层存在"
}

run_sync() {
  validate_sync_inputs
  current_app_metadata
  WORK_DIR="$(mktemp -d "${TMPDIR:-/tmp}/qime-sync.XXXXXX")"

  deploy_to_staging

  # 在确认部署产物完整后才备份和修改 App。
  create_versioned_backup

  log "同步星猫键道源码、Lua 与 OpenCC……"
  copy_source_overlay
  copy_build_overlay

  # Qianwen 固定加载 schema_id=qw，因此源 schema 和部署 schema 都要生成 qw 别名。
  make_qw_schema "$RIME_DIR/xmjd6.schema.yaml" "$QW_SCHEMAS/qw.schema.yaml"
  make_qw_schema "$WORK_DIR/build/xmjd6.schema.yaml" "$QW_BUILD/qw.schema.yaml"
  copy_qime_support_files

  if [[ "$NO_RELOAD" -eq 0 ]]; then
    [[ -x "$QW_RELAUNCHER" ]] || die "找不到官方重载 helper：$QW_RELAUNCHER"
    log "使用千问官方 helper 重载输入法……"
    "$QW_RELAUNCHER" --host-bundle-path "$QW_APP"
  else
    log "已跳过重载（--no-reload）"
  fi

  write_state
  log "同步完成：千问 $APP_VERSION ($APP_BUILD)"
  log "备份：$LAST_BACKUP"
  if [[ "$WITH_RUNTIME" -eq 0 ]]; then
    log "已保留千问自己的 candidate_order.txt 和 dynamic_phrases.txt"
  fi
}

COMMAND="${1:-sync}"
if [[ $# -gt 0 ]]; then shift; fi

case "$COMMAND" in
  sync)
    while [[ $# -gt 0 ]]; do
      case "$1" in
        --with-runtime) WITH_RUNTIME=1 ;;
        --no-reload) NO_RELOAD=1 ;;
        -h|--help) usage; exit 0 ;;
        *) die "未知参数：$1" ;;
      esac
      shift
    done
    run_sync
    ;;
  status)
    [[ $# -eq 0 ]] || die "status 不接受额外参数"
    run_status
    ;;
  -h|--help|help)
    usage
    ;;
  *)
    usage >&2
    die "未知命令：$COMMAND"
    ;;
esac
```

### 12.2 `scripts/qianwen_overlay/qime_trigger_compat.lua`（权限 `0644`）

```lua
-- Compatibility shim for QianwenIME's direct-punctuation wrapper.
-- Qianwen clears and directly commits a one-byte punctuation composition after
-- Rime accepts it. A temporary duplicate survives that outer check; on the
-- next real key we remove the duplicate before the original processors run.
local kAccepted, kNoop = 1, 2
local triggers = { ['=']=true, [';']=true, ['`']=true, ["'"]=true, ['\\']=true }

local function key_char(key)
  local code = key and key.keycode
  if code and code >= 0x20 and code < 0x7f then return string.char(code) end
  local repr = key and key.repr and key:repr() or ''
  if #repr == 1 then return repr end
  return ({ equal='=', semicolon=';', grave='`', apostrophe="'", backslash='\\' })[repr]
end

local function processor(key, env)
  if not key then return kNoop end
  local context = env and env.engine and env.engine.context
  if not context then return kNoop end

  local input = context.input or ''
  if #input == 2 and input:sub(1,1) == input:sub(2,2)
      and triggers[input:sub(1,1)] then
    pcall(function() context:pop_input(1) end)
    return kNoop
  end

  if key:release() or key:ctrl() or key:alt() or key:super() then return kNoop end
  local ch = key_char(key)
  if ch == "'" and type(context.get_option) == 'function'
      and not context:get_option('sentence_mode_enabled') then
    return kNoop
  end
  if input == '' and triggers[ch] then
    local ok = pcall(function() context:push_input(ch .. ch) end)
    return ok and kAccepted or kNoop
  end
  return kNoop
end

return { func = processor }
```

### 12.3 `default.yaml`（权限 `0644`）

```yaml
# Rime default settings
# 注意：此文件可能会被 Rime 重新部署时覆盖
# 建议将自定义设置放在 default.custom.yaml 中

config_version: "0.40"

schema_list:
  - schema: xmjd6
  - schema: english

switcher:
  caption: "〔方案選單〕"
  hotkeys:
    - Control+Escape
    - Control+F4
    - F4
  save_options:
    - full_shape
    - jffh
    - tofu
    - embeded_cands
    - emoji_cn
    - ascii_punct
    - wxw_hint
    - completion
    - eng_space

menu:
  page_size: 6

ascii_composer:
  good_old_caps_lock: true
  switch_key:
    Caps_Lock: null
    Control_L: noop
    Control_R: noop
    Shift_L: null
    Shift_R: null

key_binder:
  bindings:
    - { when: composing, accept: Escape, send: Escape }
    - { when: has_menu, accept: minus, send: Page_Up }
    - { when: has_menu, accept: equal, send: Page_Down }

punctuator:
  full_shape:
    " ": { commit: "　" }
  half_shape:
    " ": { commit: " " }

recognizer:
  patterns:
    uppercase: "[A-Z][-_+.'0-9A-Za-z]*$"
```

### 12.4 `tests/test_sync_qianwen_rime.sh`（权限 `0755`）

```bash
#!/bin/bash
set -euo pipefail

SCRIPT="/Users/mac/Library/Rime/scripts/sync_qianwen_rime.sh"
TMP_ROOT="$(mktemp -d "${TMPDIR:-/tmp}/test-qime-sync.XXXXXX")"
trap 'rm -rf "$TMP_ROOT"' EXIT

fail() {
  printf 'FAIL: %s\n' "$*" >&2
  exit 1
}

assert_file() {
  [[ -f "$1" ]] || fail "missing file: $1"
}

assert_contains() {
  local file="$1"
  local text="$2"
  grep -Fq -- "$text" "$file" || fail "$file does not contain: $text"
}

assert_not_contains() {
  local file="$1"
  local text="$2"
  if grep -Fq -- "$text" "$file"; then
    fail "$file unexpectedly contains: $text"
  fi
}

assert_equal_files() {
  cmp -s "$1" "$2" || fail "files differ: $1 <> $2"
}

APP="$TMP_ROOT/QianwenIME.app"
DATA="$APP/Contents/SharedSupport/qw_ime_data"
SCHEMAS="$DATA/schemas"
BUILD="$DATA/rime_user/build"
QIME="$TMP_ROOT/Qime"
RIME="$TMP_ROOT/Rime"
FAKE_BUILD="$TMP_ROOT/fake-build"
BACKUPS="$TMP_ROOT/backups"
STATE="$TMP_ROOT/state"
MOCK_BIN="$TMP_ROOT/bin"

mkdir -p "$SCHEMAS/lua/xmjd6" "$BUILD" "$QIME" \
  "$RIME/lua/xmjd6" "$RIME/opencc" "$FAKE_BUILD" "$MOCK_BIN"

cat >"$APP/Contents/Info.plist" <<'PLIST'
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0"><dict>
  <key>CFBundleShortVersionString</key><string>1.2.3</string>
  <key>CFBundleVersion</key><string>456</string>
</dict></plist>
PLIST

printf 'vendor-default\n' >"$SCHEMAS/default.yaml"
printf 'old-qw-schema\n' >"$SCHEMAS/qw.schema.yaml"
printf 'old-build\n' >"$BUILD/qw.table.bin"
printf 'runtime-order\n' >"$QIME/candidate_order.txt"
printf 'runtime-phrase\n' >"$QIME/dynamic_phrases.txt"

cat >"$RIME/default.yaml" <<'YAML'
schema_list:
  - schema: xmjd6
YAML

cat >"$RIME/xmjd6.schema.yaml" <<'YAML'
schema:
  schema_id: xmjd6
engine:
  processors:
    - ascii_composer
translator:
  dictionary: xmjd6.extended
sentence_mode:
  dictionary: xmjd6.extended
YAML

printf 'name: xmjd6.extended\n' >"$RIME/xmjd6.extended.dict.yaml"
printf 'name: xmjd6.cx\n' >"$RIME/xmjd6.cx.dict.yaml"
printf 'name: xmjd6.fuhao\n' >"$RIME/xmjd6.fuhao.dict.yaml"
printf 'return {}\n' >"$RIME/lua/xmjd6/example.lua"
printf '{"name":"test"}\n' >"$RIME/opencc/test.json"
printf 'source-order\n' >"$RIME/candidate_order.txt"
printf 'source-phrase\n' >"$RIME/dynamic_phrases.txt"
printf 'source-anniversary\n' >"$RIME/anniversaries.txt"

cp "$RIME/xmjd6.schema.yaml" "$FAKE_BUILD/xmjd6.schema.yaml"
printf 'prism-data\n' >"$FAKE_BUILD/xmjd6.extended.prism.bin"
printf 'reverse-data\n' >"$FAKE_BUILD/xmjd6.extended.reverse.bin"
printf 'table-data\n' >"$FAKE_BUILD/xmjd6.extended.table.bin"
printf 'cx-data\n' >"$FAKE_BUILD/xmjd6.cx.table.bin"

cat >"$MOCK_BIN/rime_deployer" <<'SH'
#!/bin/bash
set -euo pipefail
[[ "$1" == "--build" ]]
mkdir -p "$4"
cp "$FAKE_BUILD_DIR"/* "$4/"
printf '%s\n' "$*" >>"$DEPLOY_LOG"
SH
chmod +x "$MOCK_BIN/rime_deployer"

cat >"$MOCK_BIN/relaunch" <<'SH'
#!/bin/bash
set -euo pipefail
printf '%s\n' "$*" >>"$RELAUNCH_LOG"
SH
chmod +x "$MOCK_BIN/relaunch"

run_sync() {
  QW_APP="$APP" \
  QW_USER_DIR="$QIME" \
  RIME_DIR="$RIME" \
  BACKUP_ROOT="$BACKUPS" \
  STATE_DIR="$STATE" \
  RIME_DEPLOYER="$MOCK_BIN/rime_deployer" \
  SQUIRREL_SHARED="$TMP_ROOT/squirrel-shared" \
  QW_RELAUNCHER="$MOCK_BIN/relaunch" \
  FAKE_BUILD_DIR="$FAKE_BUILD" \
  DEPLOY_LOG="$TMP_ROOT/deploy.log" \
  RELAUNCH_LOG="$TMP_ROOT/relaunch.log" \
  "$SCRIPT" "$@"
}

mkdir -p "$TMP_ROOT/squirrel-shared"

run_sync sync

BACKUP_DIR="$(find "$BACKUPS" -maxdepth 1 -type d -name 'qime-pre-sync-1.2.3-456-*' | head -1)"
[[ -n "$BACKUP_DIR" ]] || fail "versioned pre-sync backup was not created"
assert_contains "$BACKUP_DIR/qw_ime_data/schemas/qw.schema.yaml" "old-qw-schema"

assert_contains "$SCHEMAS/qw.schema.yaml" "schema_id: qw"
assert_contains "$SCHEMAS/qw.schema.yaml" "dictionary: qw"
assert_contains "$SCHEMAS/qw.schema.yaml" "lua_processor@*xmjd6/qime_trigger_compat"
[[ "$(grep -Fc 'lua_processor@*xmjd6/qime_trigger_compat' "$SCHEMAS/qw.schema.yaml")" -eq 1 ]] \
  || fail "compat processor was inserted more than once"
assert_not_contains "$SCHEMAS/qw.schema.yaml" "dictionary: xmjd6.extended"
assert_contains "$SCHEMAS/default.yaml" "vendor-default"

assert_file "$SCHEMAS/lua/xmjd6/qime_trigger_compat.lua"
assert_equal_files "$RIME/lua/xmjd6/example.lua" "$SCHEMAS/lua/xmjd6/example.lua"
assert_equal_files "$RIME/opencc/test.json" "$SCHEMAS/opencc/test.json"
assert_equal_files "$FAKE_BUILD/xmjd6.extended.table.bin" "$BUILD/qw.table.bin"
assert_equal_files "$FAKE_BUILD/xmjd6.extended.prism.bin" "$BUILD/qw.prism.bin"
assert_equal_files "$FAKE_BUILD/xmjd6.extended.reverse.bin" "$BUILD/qw.reverse.bin"
assert_equal_files "$RIME/xmjd6.cx.dict.yaml" "$QIME/xmjd6.cx.dict.yaml"
assert_equal_files "$RIME/xmjd6.fuhao.dict.yaml" "$QIME/xmjd6.fuhao.dict.yaml"

# Runtime learning/state is deliberately preserved unless explicitly requested.
assert_contains "$QIME/candidate_order.txt" "runtime-order"
assert_contains "$QIME/dynamic_phrases.txt" "runtime-phrase"

assert_file "$STATE/last-sync.env"
assert_contains "$STATE/last-sync.env" "app_version=1.2.3"
assert_contains "$STATE/last-sync.env" "app_build=456"
assert_file "$TMP_ROOT/deploy.log"
awk '$1 == "--build" && $2 == $3 {found=1} END {exit !found}' "$TMP_ROOT/deploy.log" \
  || fail "deployer must use the staged Rime source as shared data, not Squirrel presets"
assert_file "$TMP_ROOT/relaunch.log"

run_sync status >"$TMP_ROOT/status-current.log"
assert_contains "$TMP_ROOT/status-current.log" "已同步"

# A new app build must be reported as needing a re-sync.
plutil -replace CFBundleVersion -string 457 "$APP/Contents/Info.plist"
set +e
run_sync status >"$TMP_ROOT/status-updated.log" 2>&1
STATUS_RC=$?
set -e
[[ "$STATUS_RC" -eq 10 ]] || fail "updated app status exit code: $STATUS_RC (expected 10)"
assert_contains "$TMP_ROOT/status-updated.log" "检测到千问更新"

# Explicit runtime synchronization is opt-in.
run_sync sync --with-runtime
assert_contains "$QIME/candidate_order.txt" "source-order"
assert_contains "$QIME/dynamic_phrases.txt" "source-phrase"

printf 'PASS: qianwen rime sync\n'
```

---

## 13. 维护规则

以后如果修改同步脚本或兼容 Lua，应同时更新：

1. 第 3、4 节的行为说明。
2. 第 8 节验收项。
3. 第 10 节字节数和 SHA-256。
4. 第 12 节对应源码载荷。
5. 第 6 节当前实机版本、identity、备份和同步时间（仅在确实重新同步后更新）。

本文件记录的是“如何重建修改”，不是用户学习数据备份。`candidate_order.txt`、`dynamic_phrases.txt`、Rime userdb 和千问 `Qime` 目录仍应由各自同步/备份机制保存。

---

## 14. 2026-08-11：按键语义兼容引擎在千问 1.2.1.30 失效

### 结论

`~/Library/Application Support/QianwenIME/RimeSync/compat/libqime-key-semantics.dylib`
**不是补丁产物，而是旧版千问（1.1.5.23 时期）的厂商原版 `libqime.dylib`**，
由 `Developer ID Application: Shanghai Zhixin Puhui Technology Co., Ltd. (8T9NQJXDU3)` 签名。
它能被千问加载，靠的正是这份厂商签名。

千问 1.2.1.30 的 `libqianwen_engine.dylib` 新增了 5 个快捷短语接口调用：

```
__ZN4qime10QimeEngine14AddQuickPhraseERKNS_20QimeQuickPhraseDraftE
__ZN4qime10QimeEngine17DeleteQuickPhraseENSt3__117basic_string_viewIcNS1_11char_traitsIcEEEE
__ZN4qime10QimeEngine17QueryQuickPhrasesEv
__ZN4qime10QimeEngine17UpdateQuickPhraseERKNS_15QimeQuickPhraseE
__ZN4qime10QimeEngine28GenerateQuickPhraseInputCodeENSt3__117basic_string_viewIcNS1_11char_traitsIcEEEE
```

旧 libqime 没有这些符号，装进去千问会因符号缺失起不来，所以 ABI 校验拦截是正确的。

### 三条补救路径的验证结果

| 方案 | 结论 |
|---|---|
| 给旧 libqime 补桩符号后自签名 | ❌ App 为 hardened runtime 且无 `com.apple.security.cs.disable-library-validation`，非同 Team ID 的 dylib 无法加载 |
| 连 `libqianwen_engine.dylib` 一起回退到旧版 | ❌ 主程序 `Contents/MacOS/QianwenIME` 需要 32 个旧包装层没有的符号（quick_phrase / clipboard / voice） |
| 跳过引擎替换，只同步 Rime 数据 | ✅ 已采用 |

**没有厂商私钥就无法为新版千问重建这个兼容引擎。** 未来千问再更新，此结论不变；
只有厂商自己改回旧的按键语义，或用户回退到旧版千问，兼容引擎才会重新可用。

### 脚本行为变更

- `decide_engine_compat` 在**改动任何千问文件之前**决定兼容引擎去留。
- 兼容引擎不可用（ABI 缺失 / 签名无效 / Team ID 不符 / 架构不符）时，默认打印警告并
  **跳过 libqime 替换，继续完成 Rime 数据同步**，不再整体中止。
- `QW_ENGINE_COMPAT_STRICT=1` 恢复旧的硬失败行为，且在备份和改文件之前就退出。
- 跳过引擎替换时不再要求 `Contents/Frameworks` 可写，因此通常不必再走原子暂存整包替换。
- `last-sync.env` 新增 `engine_compat_status=installed|skipped|disabled|absent`；
  `status` 只在 `installed` 时校验 libqime 哈希，跳过时返回 `0` 并说明使用千问自带引擎。
- 顺带修复 `"$VAR；"` 形式的变量展开：全角标点会被 bash 当成变量名的一部分，
  在 `set -u` 下报 unbound variable。新增代码及原有的“部署结果缺少 `$required`”一处均改为 `${VAR}`。

### 实机状态

- 千问版本：`1.2.1`，构建号：`1.2.1.30`
- App identity：`f40edf7c42ef8e0c3e5d892d7b2f56e621f79a3c`
- 同步时间：`2026-08-11 16:40:46 +0800`
- 备份：`.../Backups/RimeSync/qime-pre-sync-1.2.1-1.2.1.30-f40edf7c42ef`
- `libqime.dylib` 保持官方原版 `4e2dd1171f69059c5c3bc905c551c2a8a7c7eb5d6ab0a6f30142b686a6b937a9`
- `status` 输出：`已同步：千问 1.2.1 (1.2.1.30)，星猫键道就绪；按键语义兼容引擎未启用（skipped），使用千问自带引擎`

### 待用户实测

顶功、`=` `;` 反引号 `'` `\` 引导键、单符号显示在千问自带引擎下是否仍正常。
若失效，可用的回退副本：
`~/Library/Application Support/QianwenIME/Backups/QianwenIME-stock-20260724-191833.app`（1.1.5.23 整包）。

### 实测结果：千问 1.2.x 下 processor 类功能全部失效

实测确认顶功和 `=` 引导键都不可用。用探针（在 `qime_trigger_compat.lua` 即 processor 链
**第一个**处理器里记录每一个到达的按键）取得决定性证据——用户完整敲了 `nkhznkhz` 和 `=uuid`，
到达 librime 的按键只有：

```
code=61 repr=equal      code=65288 repr=BackSpace ×5
code=65293 repr=Return ×2   code=65307 repr=Escape   code=44 repr=comma
```

**一个字母键都没有**，且 `input` 多数为空。结论：

- 千问 1.2.x 把字母输入完全留在自己那一层，只把控制键和标点转发给 librime 的 `ProcessKeyEvent`；
  字母是整串灌进引擎的（对应 `QimeSession::InputText(std::string_view)`）。
- 因此**所有 `lua_processor` 对字母输入都不再触发**：顶功、`=` 引导键、`0` 调频、
  以词定字、快捷符号、空码直接上屏、`auto_fallback` 等全部失效。
- librime 原生的 `speller/auto_select`（另一种顶功实现）**同样无效**——`speller` 也是 processor，
  位于探针下游，探针都收不到字母键，speller 更收不到。
- 仍然有效的只有 segmentor / translator / filter，所以候选还能正常出。
- schema 每次输入事件都被重新初始化（一次输入观察到 28 次 lua `init`），说明引擎被反复重建。

这不是配置问题，是架构变更，**Lua 与 schema 层面无解**。

### 处置：回退到 1.1.5.23 并禁用自动更新

新增 `scripts/rollback_qianwen.sh`，用备份的 App 整包替换当前千问：

- 替换前自动整包克隆当前版本到 `Backups/QianwenIME-stock-<版本>-<构建号>.app`，并备份 `Qime` 用户目录。
- 在**暂存副本**里把 `Contents/Helpers/QianwenIMEUpdater` 改名为 `.disabled`，
  再整体换入——因为 App Management 不允许直接改现役 App 内的文件。
- 复用千问自带的厂商签名 `QianwenIMEAtomicSwap` + launchd 一次性任务交换 `Contents`，
  与 `sync_qianwen_rime.sh` 的原子暂存同一机制。
- `install <App 路径>` 可随时换回新版。

2026-08-11 实机执行结果：

- 千问：`1.1.5 (1.1.5.23)`，自动更新已禁用
- `sync` 输出：`已同步：千问 1.1.5 (1.1.5.23)，星猫键道就绪；逐键兼容层存在`
- 1.1.5.23 自带的 `libqime.dylib` 与 compat 文件哈希完全相同（`893c5b88…`），
  这也最终证明 compat 就是该版本的厂商原件
- 新版整包备份：`Backups/QianwenIME-stock-1.2.1-1.2.1.30.app`

### 调试探针

`lua/xmjd6/xmjd6_topup_processor.lua` 和 `scripts/qianwen_overlay/qime_trigger_compat.lua`
保留了带开关的调试日志：仅当 `/tmp/xmjd6_topup_debug` 存在时写入
`/tmp/xmjd6_topup.log` 和 `/tmp/xmjd6_keys.log`，平时只多一次布尔判断。
以后千问再改按键路径，用它可以一次性判定按键是否还进 processor 链。
