# Task completion checklist

Before claiming work complete:
- Re-run a focused verification command for the changed files.
- Review `git diff` for unintended scope changes.
- If packaging behavior changed, verify the relevant exclude/include patterns in the target scripts.
- Mention clearly if build/package scripts were not executed end-to-end.
